language: node_js

notifications:
  email: false

cache: yarn

node_js:
  - '8'
  - '9'

script: skip

jobs:
  include:
    - stage: 📋 Test
      script: npm test
      node_js: '8'
      script: npm test
      node_js: '9'
    - stage: 🚀 Publish
      script: npm run deploy
      if: branch = master
