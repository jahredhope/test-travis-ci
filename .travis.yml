language: node_js

notifications:
  email: false

jobs:
  include:
    - stage: 📋 Test
      script: npm test
      node_js:
        - '8'
        - '9'
      env:
        - FOO=foo
        - FOO=bar
    - stage: 🚀 Publish
      test: 'echo "Not testing publish"'
      script: npm run deploy
      node_js:
        - '8'

stages:
  - 📋 Test
  - name: 🚀 Publish
    if: branch = master
