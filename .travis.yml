language: node_js

notifications:
  email: false

cache: yarn

node_js:
  - '6'
  - '8'
  - '9'
env:
  - FOO=foo
  - FOO=bar

jobs:
  include:
    - stage: 🚀 Publish
      script: npm run deploy
      node_js:
        - '8'

stages:
  - name: 🚀 Publish
    if: branch = master
