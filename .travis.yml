language: node_js

notifications:
  email: false

cache: yarn

node_js:
  - '8'
  - '9'

stage: 📋 Test

jobs:
  include:
    - stage: 🚀 Publish
      script: npm run deploy
      if: branch = master

stages:
  - 📋 Test
  - 🚀 Publish
